<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="row">
      <div class="large-6 large-centered columns">
        <% flash.each do |key, value| %>
          <div class="alert alert-box rounded"><%= value %></div>
        <% end %>
      </div>
    </div>

    <%= yield %>
    <% if current_user %>
      <div class="center">
        <b>Signed In as <%= current_user.email %></b>
        <%= link_to "sign out", destroy_user_session_path, method: :delete, class: "button info radius tiny" %>
        <%= link_to "home", home_path, class: "button info radius tiny" %>
      </div>
    <% end %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
